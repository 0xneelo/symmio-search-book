# Search Book Final Report

Generated for the standalone `symmio-search-book` repository on 2026-06-30 and refreshed on 2026-07-01 after the migration to `main`, the operator-blocker reconciliation, the real Discord corpus import, and the sanitized Discord routing summary. This report is intentionally explicit about remaining production work: the Search Book dossier, prototype, source corpus, and answer runtime are verified, but the overall mission is not production-complete until the production VPS env and public deploy route are resolved or carried into an approved release plan.

For the strict requirement-by-requirement status, see `COMPLETION-AUDIT.md`. It treats the current build as preview-ready, not production-complete.

## Current Status

The Search Book now has a 500-800 page compendium shape with 794 manifest pages, 801 authored pages, 8 volumes, 104 chapters, and 1,595 reader-routable pages assigned to volumes. The page-state registry reports 800 published public-navigation pages, 792 source companions, and 3 internal drafts. All 820 exact public question routes point to published public-navigation pages; there are 0 candidate pages remaining. The generated corpus stays inside the requested 500-800 page target while the reader layer includes authored pages plus source companions for traceability.

The authored layer now covers every generated source companion: `data/publication-plan.*` reports 792 source companions queued, 792 covered by authored pages, 0 needing authored coverage, and 0 candidate pages still in the review queue. The remaining candidate review lanes are 0 final-review-ready pages, 0 operator-review pages, 0 source-refresh pages, 0 publication-date-review pages, and 0 editorial-review pages. Source companions remain retrieval and traceability material, not public navigation pages.

The answer-engine front door is implemented as a static prototype plus a shared runtime and standalone SQLite-backed service boundary. The deterministic route map has 820 exact question routes, 32 glossary routes, 2 refusal routes, 2,884 retrieval chunks, and 822 local FAQ entries. The static prototype can be opened directly at `index.html` from the standalone repo root or served locally with `npm run search-book:serve-static`; `npm run search-book:check-static` verifies local script dependencies, expected `window.SearchBook*` data globals including page-state metadata, static page links, and public reader-data coverage, while `npm run search-book:smoke-static` proves the Ask front door, exact-page URL, generated data assets, and missing-route 404 behavior over localhost. `npm run search-book:check-publication-boundaries` now proves public Browse/navigation, exact routes, FAQ routes, and volume entry links stay on public pages; source companions stay traceability/retrieval-only; and internal drafts stay out of public navigation and runtime context. The service can be run locally with `SEARCH_BOOK_ANSWER_ENGINE_DB=/tmp/search-book-answer-engine.sqlite node scripts/serve-answer-engine.mjs` and connected with `index.html?service=http://127.0.0.1:8787`. The local smoke command `npm run search-book:smoke-service` proves service health, extractive answer persistence, answer-rating persistence, reader page-feedback persistence, helpful answer-cache population, paraphrase reuse as `source:"reuse-cache"`, optional dynamic examples from helpful cached questions, Search Insights, guardrail refusal ordering, token-gated moderation export, and the internal reviewer gap-summary job against a temporary SQLite database. `npm run search-book:check-monitoring` validates `/health` plus token-gated `/api/search-book/metrics`, requiring production-safe service URLs and metrics tokens in production while offering a no-secret local mode. `npm run search-book:backup-db` creates a SQLite-consistent answer-engine backup, writes an immutable restore-checked manifest, and can refresh the latest manifest consumed by launch readiness; `deploy/symmio-search-book-backup.service` plus `deploy/symmio-search-book-backup.timer` provide the production daily timer template. `npm run search-book:check-production-env` fails closed until the production VPS service env at `/etc/symmio-search-book/search-book.env`, safe CORS, reviewer owner/cadence, metrics token, and backup storage are configured without printing secret values. Local `.secrets/search-book.env` is complete for local LLM work and must not be printed or treated as a missing operator input. `npm run search-book:smoke-deployment` is the URL-driven preview/staging/production smoke: read-only by default for static assets and service health/CORS/Search Insights, with explicit `--write` required before creating one answer event, answer rating, and reader page-feedback event. `npm run search-book:check-launch` is the executable production/staging launch gate, composing production env preflight, deterministic verify, deployment smoke, reviewer owner/cadence evidence, backup-storage evidence, latest restore-checked backup manifest evidence, and unresolved completion-boundary checks without printing secret values. `LIVING-DOCS-OPERATIONS.md` documents the internal daily/weekly reviewer workflow, moderation export handling, privacy boundaries, launch gate, and incident response. The combined `npm run search-book:smoke-preview-service` command starts both localhost services, checks the preview-to-service CORS bridge, and verifies service-backed ask, answer rating, reader page feedback, Search Insights, and exact-page URLs without live LLM calls; with `--static-root` it runs the same bridge smoke against a copied static artifact.

Current localhost preview evidence includes the reusable `npm run search-book:drill-local-launch` command, which starts temporary static and answer-engine services, records one smoke answer, answer rating, and reader page-feedback event, creates a restore-checked backup manifest, runs staging launch readiness with fresh verify and write-smoke, and tears the services down without loading LLM credentials. The latest local launch drill on 2026-07-01 passed with deployment write-smoke recording answer `answered`, rating `recorded`, pageFeedback `recorded`, 2 citations, and launch readiness 15/15 staging checks. `npm run search-book:launch-evidence` packages the same no-secret proof into `launch-evidence.json` and `launch-evidence.md` under `/tmp` by default, now including first-class `/health` plus token-gated `/api/search-book/metrics` monitoring evidence, Vibe public-docs source-freshness evidence, status-document evidence, Discord review-artifact evidence for the no-raw editorial queue, Discord refusal-runtime evidence for the public-safe refusal probes, and publication-boundary evidence for public/source/internal page-state separation, and can wrap production launch readiness when the final URLs, production env, and latest backup manifest are available. The latest clean no-secret release rehearsal at `/tmp/search-book-release-dry-run-clean-repo-discord-refusal-runtime-evidence-20260701-1` passed with release and nested launch repository dirty state `false` from the same commit, static artifact `passed`, copied-bundle integrity `passed`, launch status `passed`, monitoring status `passed`, source freshness status `passed`, status evidence status `passed`, Discord review artifacts status `passed`, Discord refusal runtime status `passed`, publication boundaries status `passed`, evidence summary renderer status `passed`, status evidence documents `4/4`, `valuesPrinted:false`, 0 sensitive-pattern matches, living-docs controls all true including `pageFeedback:true`, source ingestion as 17 complete / 0 partial / 0 parked / 0 missing, Discord route coverage at 19/19 page-fit groups with 0 single-route groups, 0 groups without public routes, and 40 public exact routes, source-backed triage at 19/19 page-fit groups, public-copy ready for 19/19 page-fit groups, refusal policy ready at 2/2 refusal items, refusal policy review required 0/2 refusal items, Discord refusal runtime probes at 2/2 refused with `discord-corpus-review-required`, `G-001`, zero citations, zero answer bytes, and no LLM credentials loaded, Discord editorial queue evidence at 24 routed review items, 19 page-fit groups, 2 refusal-review items, 2/2 refusal-policy-ready items, 0 refusal-policy review-required items, 0 raw-key hits, 0 sample leaks, and 0 queue raw-table hits, publication-boundary evidence at 800 public navigation pages, 792 source companion pages, 820 exact routes, 820 FAQ routes, 1,321 source-companion runtime chunks, 0 internal-draft runtime chunks, and 11/11 publication checks, and evidence summary renderer output at 26 launch lines / 30 release lines without printed values. A newer current-commit rehearsal at `/tmp/search-book-release-dry-run-monitoring-verify-gate-20260701-1` also passed from clean commit `412bd6a` with release and nested launch repository dirty state `false`, static artifact `1,650` files / `52,819,821` bytes, launch/monitoring/source-freshness/status-evidence/Discord-review-artifacts/Discord-refusal-runtime/publication-boundaries/evidence-summary-renderer all `passed`, `valuesPrinted:false`, 0 sensitive-pattern matches, source ingestion 17/17, and only #11/#4 open. `npm run search-book:check-launch-evidence-packet -- --packet /tmp/search-book-release-dry-run-clean-repo-discord-refusal-runtime-evidence-20260701-1/launch-evidence/launch-evidence.json` and `npm run search-book:check-release-dry-run-packet -- --packet /tmp/search-book-release-dry-run-clean-repo-discord-refusal-runtime-evidence-20260701-1/release-dry-run.json` passed against the nested launch packet and release packet, including the repository-clean checks, same-commit check, source-backed Discord triage, public-copy readiness, refusal-policy readiness checks, Discord refusal-runtime checks, and publication-boundary packet checks. Targeted packet assertions confirmed the source-freshness evidence carries statuses, hashes, and booleans without normalized source text or Markdown bodies, status evidence confirms `FINAL-REPORT.md`, `COMPLETION-AUDIT.md`, `PRODUCTION-READINESS-PACKET.md`, and `README.md` contain current generated counts with open operator items limited to #4/#11, Discord review-artifact evidence confirms the committed Search Insights summary plus `DISCORD-EDITORIAL-QUEUE.md` stay no-raw, route-covered, source-backed triage-ready, public-copy-ready, and refusal-policy-ready, Discord refusal-runtime evidence confirms the actual answer runtime keeps both public-safe Discord/Lafa probes refusal-only without loading LLM credentials, and publication-boundary evidence is count-only. Earlier localhost preview evidence also used `http://127.0.0.1:8798/?service=http%3A%2F%2F127.0.0.1%3A8797&serviceMode=extractive`, backed by the extractive answer-engine service at `http://127.0.0.1:8797`; that staged launch drill passed with 13 checks, 0 failures, and 3 expected staging warnings for reviewer owner, review cadence, and backup storage assignment.

The standalone repository now has a no-secret GitHub Actions gate at `.github/workflows/search-book-verify.yml`. On `main` pushes and pull requests it clones the public `0xneelo/vibe_docs` export into `/tmp/vibe_docs`, then runs `npm run search-book:verify`, `npm run search-book:smoke-static`, `npm run search-book:smoke-service`, and `npm run search-book:smoke-preview-service` without loading LLM credentials, production env files, moderation tokens, metrics tokens, or Discord tokens. Companion manual workflows at `.github/workflows/search-book-launch-evidence.yml`, `.github/workflows/search-book-static-artifact.yml`, and `.github/workflows/search-book-release-dry-run.yml` build the no-secret launch-evidence packet, platform-neutral static preview bundle, and combined release dry-run packet; the artifact paths smoke-test the copied bundle both as static files and against a temporary answer-engine service before uploading short-lived review artifacts. The launch and release workflows now call checked-in packet validators instead of inline one-off assertions, including explicit checks for source freshness, status-document evidence, Discord review-artifact evidence, and publication-boundary evidence, then write `$GITHUB_STEP_SUMMARY` tables with count-only operator evidence for those same fields. The publication-day Vibe source freshness check is executable as `npm run search-book:check-source-freshness`; the latest 2026-07-01 run fetched the two registered official Vibe Markdown sources, passed 4/4 market/leverage wording checks, and printed only statuses, hashes, and booleans.

The live OpenAI-compatible RAG runtime passed the current recorded SYN-215 eval on 2026-07-01 with `gpt-4.1-mini`: 42/42 total cases, 15/15 adversarial refusals, and 27/27 answer-validation fixtures. Measured usage was 16 calls, 95,037 input tokens, 8,264 output tokens, and an estimated cost of `$0.01921395` at the recorded `gpt-4.1-mini` pricing. This is runtime evidence, not a deployed-service readiness claim.

Quality status is still intentionally not green for launch. The latest audit passes 29/30 gates. The only failing gate is `operator-inbox`, reflecting the two remaining production operator gates. The regenerated source-ingestion map now reports 17/17 requirements complete, 0 partial, 0 parked, and 0 missing. The Discord/Lafa corpus imports 5,000 messages, 723 question clusters, and 837 configured Lafa answer candidates in internal-only mode with no raw or normalized Discord text stored in checked-in data. The committed Discord routing summary maps 24 local review items to existing answer behavior without carrying raw Discord questions, raw Lafa/source answers, or generated answer text into static data; Search Insights displays 22 answered existing-page fits, grouped into 19 page-fit checks, plus 2 refusal-review items as editorial evidence. `DISCORD-EDITORIAL-QUEUE.md` now mirrors that sanitized review plan as a no-raw Markdown work queue with item ids, page ids, page titles, source keys, route counts, public-route coverage, automated triage status, public-copy readiness status, refusal policy status, and refusal reasons only. It also reports that 19/19 page-fit groups now have public route aliases, 0 single-route groups remain, those groups have 40 public exact routes between them, source-backed triage at 19/19 page-fit groups, public-copy ready for 19/19 page-fit groups, public-copy review required for 0/19 page-fit groups, refusal policy ready at 2/2 refusal items, and refusal policy review required for 0/2 refusal items. `npm run search-book:check-discord-review-artifacts` now validates the committed summary and the Markdown queue, and can validate the local `/tmp` raw review plus sanitized routing packets without printing excerpts; the latest run passed with 24 routed items, zero raw-key hits, zero sample leaks, 19/19 route coverage, 19/19 source-backed triage, 19/19 public-copy readiness, 2/2 refusal policy ready, 0/2 refusal policy review-required, queue page-fit/refusal ids present, zero raw queue table markers, and the raw packet path remaining outside the repo when supplied. `npm run search-book:check-discord-refusals` now exercises the actual extractive answer runtime for two public-safe Discord/Lafa probes and requires both to return `refusal`, `discord-corpus-review-required`, `G-001`, zero citations, zero answer bytes, no primary page, and no LLM credentials loaded. The regenerated definition-of-done map reports 14/18 requirements complete, 2 partial, 2 parked, and 0 missing. It remains not completion-ready because production deployment and production service configuration still depend on #11 and #4.

## Verification Evidence

Use these commands as the current reproducible verification path:

```sh
node scripts/build-all.mjs --verify
npm run search-book:verify
node scripts/check-readiness-evidence.mjs
node scripts/check-static-integrity.mjs
npm run search-book:check-publication-boundaries
npm run search-book:check-source-freshness
npm run search-book:check-completion-audit
npm run search-book:check-production-env-fixture
npm run search-book:check-deploy-templates
npm run search-book:check-monitoring
npm run search-book:check-launch-evidence-packet -- --packet /tmp/search-book-release-dry-run-clean-repo-discord-refusal-runtime-evidence-20260701-1/launch-evidence/launch-evidence.json
npm run search-book:check-release-dry-run-packet -- --packet /tmp/search-book-release-dry-run-clean-repo-discord-refusal-runtime-evidence-20260701-1/release-dry-run.json
npm run search-book:check-discord-review-artifacts -- --review-json /tmp/search-book-discord-review-20260701-1/discord-review-queue.json --routing-json /tmp/search-book-discord-routing-20260701-1/discord-review-routing.json
npm run search-book:check-discord-refusals
npm run search-book:discord-editorial-queue
node --check scripts/backup-answer-engine-db.mjs
node scripts/run-llm-rag-answer.mjs --mode extractive --query "Which dashboard views are documented?" --json
npm run search-book:smoke-static
npm run search-book:smoke-preview-service
npm run search-book:smoke-service
npm run search-book:smoke-deployment -- --site-url http://127.0.0.1:<preview-port> --service-url http://127.0.0.1:<service-port> --write
npm run search-book:check-launch -- --profile staging --allow-local --site-url http://127.0.0.1:<preview-port> --service-url http://127.0.0.1:<service-port> --mode extractive --backup-manifest /tmp/search-book-backup.manifest.json --skip-production-env --run-verify
npm run search-book:check-launch -- --profile staging --allow-local --site-url http://127.0.0.1:8798 --service-url http://127.0.0.1:8797 --mode extractive --backup-manifest /tmp/search-book-localhost-8797-launch-drill.manifest.json --skip-production-env --run-verify --write-smoke
git diff --check
npm run build --if-present
```

The canonical build verifies 26 deterministic build steps, 86 syntax checks, exact-route integrity, FAQ routing, answer chunks, authored-page indexing, requirement coverage, quality-audit gates, readiness-evidence consistency against `data/llm-rag-contract.json`, static app integrity, publication-boundary integrity, living-docs summary and backup utility presence, Discord review-artifact privacy, Discord editorial queue generation and consistency, Discord/Lafa refusal runtime behavior, status-document evidence consistency, objective-level completion-audit consistency, operator-inbox consistency, no-secret production-env fixture behavior, systemd deploy-template consistency, no-secret local monitoring evidence for `/health` plus token-gated `/api/search-book/metrics`, evidence-packet validators with launch/release evidence-summary renderer proof, evidence-summary renderer no-raw/no-secret output, and the native sensitive-pattern scan. Live LLM evaluation is not part of every deterministic rebuild because it requires private service credentials and must never print the API key; its latest recorded result is stored in `data/llm-rag-contract.json`.

## Requirement Summary

| Area | Current evidence | Release status |
| --- | --- | --- |
| Page target | 794 manifest pages against the 500-800 target | Verified for dossier/prototype |
| Authored layer | 801 authored pages across manifesto, product, rewards, dashboard, protocol, answer-engine, glossary, and compendium sections; 800 published public-navigation pages; 820 exact public question routes; 0 candidate pages; 792/792 source companions covered by authored pages; 0 candidate pages ready for final review or operator review | Published corpus verified; requirement map marks manifesto/reference complete; remaining work is deploy/ops readiness |
| Dashboard reference | 13 dashboard-reference pages, including every visible dashboard route plus hidden `#revenue` route | Published with Phase A, referral-depth, volume-source, and Discord/deploy boundaries preserved |
| Answer engine | 820/820 exact routes, 32/32 glossary routes, 2/2 refusal routes | Deterministic runtime verified |
| LLM RAG | 42/42 live eval on `gpt-4.1-mini` | Runtime verified; production env/deploy still parked |
| Living docs | 12/12 event fixtures, SQLite service, frontend bridge, retention policy, helpful answer-cache reuse, dynamic examples endpoint, gated moderation export, internal reviewer summary job, backup/restore-check utility, and `LIVING-DOCS-OPERATIONS.md` | Implemented locally; production deploy/access/owner cadence still parked |
| Source ingestion | 17/17 complete, 0 partial, 0 parked; Discord/Lafa corpus imported internal-only with 5,000 messages, 723 question clusters, and 837 Lafa candidates, with raw and normalized Discord text redacted from checked-in data; `npm run search-book:discord-review` creates a local-only `/tmp` packet, `npm run search-book:discord-route-review` turns it into no-raw-text page-routing evidence, `npm run search-book:discord-routing-summary` publishes the sanitized summary into static Search Insights with 19/19 page-fit groups covered by public route aliases, 0 single-route groups remaining, source-backed triage at 19/19 page-fit groups, public-copy ready for 19/19 page-fit groups, public-copy review required for 0/19 page-fit groups, refusal policy ready at 2/2 refusal items, and refusal policy review required for 0/2 refusal items, and `npm run search-book:discord-editorial-queue` mirrors that plan into `DISCORD-EDITORIAL-QUEUE.md` | Source-complete for v1; Discord/Lafa answer claims still require editorial review before quoting or publishing exact statements |
| Competitive sweep | 49/50 official docs verified; Opyn excluded | Accepted benchmark baseline |
| Deploy preview | Static local prototype exists with `npm run search-book:serve-static`, static integrity via `npm run search-book:check-static`, static smoke via `npm run search-book:smoke-static`, combined `npm run search-book:smoke-preview-service` localhost evidence, and a 2026-07-01 staged launch drill against `127.0.0.1:8798` plus answer-engine `127.0.0.1:8797` with fresh verify and write-smoke passing | Production deploy route parked |

## Remaining Production Work

The following open items are the current production boundary. They are not re-asked here; this report records them so the next operator or agent can resume from `_specs/app-docs/OPERATOR-INBOX.md`. Only #11 and #4 remain open.

- OPERATOR-INBOX #11: install the production VPS service env at `/etc/symmio-search-book/search-book.env`. Local `.secrets/search-book.env` is complete for local live evals and must not be printed or treated as missing.
- OPERATOR-INBOX #4: choose the public frontend platform, repository owner, and deploy route. Backend architecture is already decided as a standalone service plus SQLite.

Until those items move, the Search Book should be described as a verified research dossier, static prototype, and answer-engine runtime, not as the final deployed production docs site.

## Handoff Notes

Keep edits scoped because this standalone repository is shared by multiple agents. Before each new Search Book phase, read `_local/agent-worklog.md`, `_specs/app-docs/OPERATOR-INBOX.md`, and the latest Linear issue or handoff note for the active slice.

The next highest-value production work is to install the production service env and deploy the static frontend plus standalone service behind the selected public route. After any source-corpus change, rerun `node scripts/build-all.mjs --verify` or `npm run search-book:verify`; after any prompt/runtime/deploy change, rerun the live LLM eval with the service credentials loaded through `--env-file` without printing secrets.
